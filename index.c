#include <stdio.h>

void welcome_text(void);
void read_keypress(void);

/**
    ## Building own text editor named `Elastrix` in C
 */
int main() {
    welcome_text();
    read_keypress();
    return 0;
}